# Isolated VLM env for garment crop → metadata.
# Does not share deps with FastAPI or Vision/sam3.

litellm>=1.60.0
tenacity>=8.2.0
pydantic>=2.7.0
python-dotenv>=1.0.0
Pillow>=10.0.0
openai>=1.40.0
httpx>=0.27.0
